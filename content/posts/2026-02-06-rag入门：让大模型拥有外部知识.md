---
title: "RAG入门：让大模型拥有外部知识"
date: 2026-02-06 08:00:28
author: "小欧Jacory"
category: "AI"
tags: ["RAG", "LLM", "向量数据库", "LangChain"]
readTime: 20
cover: ""
excerpt: "详解检索增强生成(RAG)技术原理、系统架构和完整实现，通过企业知识库问答案例，学习如何让大语言模型访问和利用外部知识。"
featured: false
---

## 目录

- [什么是RAG](#什么是rag)
- [为什么需要RAG](#为什么需要rag)
- [RAG工作原理](#rag工作原理)
- [系统架构设计](#系统架构设计)
- [环境准备](#环境准备)
- [实操案例：企业知识库问答](#实操案例企业知识库问答)
- [优化技巧](#优化技巧)
- [总结](#总结)

## 什么是RAG

**RAG（Retrieval-Augmented Generation，检索增强生成）** 是一种将外部知识检索与大语言模型生成能力相结合的技术架构。

传统的大语言模型（如GPT-4、Claude等）虽然具备强大的语言理解和生成能力，但存在两个核心局限：

1. **知识截止日期**：模型的训练数据有明确的时间 cutoff，无法获取最新信息
2. **幻觉问题**：模型可能会生成听起来合理但实际错误的内容

RAG 通过引入外部知识库，让模型在回答问题前先检索相关文档，基于检索到的准确信息来生成回答，从而解决上述问题。

### RAG 的核心价值

| 能力 | 传统LLM | RAG增强 |
|------|---------|---------|
| 最新知识 | ❌ 受限于训练数据 | ✅ 可实时更新知识库 |
| 准确性 | ⚠️ 可能产生幻觉 | ✅ 基于检索事实生成 |
| 可溯源 | ❌ 无法知道来源 | ✅ 可展示参考文档 |
| 私有化 | ❌ 需要训练 | ✅ 直接挂载私有文档 |

## 为什么需要RAG

### 场景1：企业知识库问答

想象一个客服场景：用户询问"公司2024年的退货政策是什么？"

- **传统LLM**：可能基于过时的训练数据回答，或者编造政策
- **RAG方案**：从企业最新的政策文档中检索准确信息，给出权威回答

### 场景2：医学文献查询

医生询问某种罕见病的最新治疗方案。

- **传统LLM**：训练数据可能不包含最新医学研究
- **RAG方案**：从PubMed等最新论文库检索相关研究

### 场景3：法律法规咨询

查询最新的税法变更。

- **传统LLM**：无法获知最新法律修订
- **RAG方案**：连接官方法律数据库，提供准确条文

## RAG工作原理

RAG 系统的工作流程可以分为三个主要阶段：

### 阶段1：文档预处理（离线）

```
原始文档 → 文本提取 → 文本分块 → 向量化 → 存储到向量数据库
```

1. **文本提取**：从PDF、Word、网页等格式提取纯文本
2. **文本分块**：将长文档切分成适当大小的段落（通常500-1000字符）
3. **向量化**：使用Embedding模型将文本转换为向量
4. **存储**：将向量存入向量数据库（如Chroma、Pinecone、Milvus）

### 阶段2：检索（在线）

```
用户问题 → 向量化 → 相似度搜索 → 返回Top-K相关文档
```

1. 将用户问题转换为向量
2. 在向量数据库中进行相似度搜索
3. 返回最相关的K个文档片段

### 阶段3：生成（在线）

```
用户问题 + 检索到的文档 → 构建Prompt → LLM生成回答
```

将检索到的上下文与用户问题一起送入大模型，生成基于事实的回答。

## 系统架构设计

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   用户提问      │────▶│   问题向量化    │────▶│  向量相似度搜索  │
└─────────────────┘     └─────────────────┘     └────────┬────────┘
                                                         │
                              ┌──────────────────────────┘
                              ▼
                    ┌─────────────────┐
                    │   向量数据库     │
                    │  (ChromaDB等)   │
                    └────────┬────────┘
                             │
                             ▼
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   展示回答      │◀────│   LLM生成回答   │◀────│  构建Prompt     │
└─────────────────┘     └─────────────────┘     └─────────────────┘
                               ▲
                               │
                    ┌──────────┴──────────┐
                    │   检索到的上下文     │
                    └─────────────────────┘
```

## 环境准备

### 安装依赖

```bash
pip install langchain langchain-openai chromadb python-dotenv
```

### 配置API密钥

创建 `.env` 文件：

```bash
OPENAI_API_KEY=your_openai_api_key_here
```

### 准备测试文档

创建一个 `docs/company_policy.txt`：

```
公司退货政策（2024年更新）：

1. 无理由退货：自购买之日起7天内可申请无理由退货
2. 质量问题退货：发现质量问题可在30天内申请退货
3. 退货流程：
   - 登录官网提交退货申请
   - 客服审核通过后寄回商品
   - 仓库验收后3个工作日内退款
4. 特殊商品：定制类商品不支持退货
```

## 实操案例：企业知识库问答

### 完整代码实现

```python
import os
from dotenv import load_dotenv
from langchain_openai import OpenAIEmbeddings, ChatOpenAI
from langchain.vectorstores import Chroma
from langchain.text_splitter import CharacterTextSplitter
from langchain.chains import RetrievalQA

# 加载环境变量
load_dotenv()

# 1. 准备文档
def load_documents():
    """加载文档并分割成块"""
    # 读取文档
    with open('docs/company_policy.txt', 'r', encoding='utf-8') as f:
        text = f.read()
    
    # 文本分割
    text_splitter = CharacterTextSplitter(
        separator="\n",
        chunk_size=200,
        chunk_overlap=50,
        length_function=len
    )
    
    chunks = text_splitter.split_text(text)
    return chunks

# 2. 创建向量数据库
def create_vector_store(chunks):
    """创建向量数据库"""
    # 使用OpenAI的Embedding模型
    embeddings = OpenAIEmbeddings()
    
    # 创建Chroma向量数据库
    vector_store = Chroma.from_texts(
        texts=chunks,
        embedding=embeddings,
        persist_directory="./chroma_db"
    )
    
    # 持久化存储
    vector_store.persist()
    return vector_store

# 3. 构建RAG问答链
def create_qa_chain(vector_store):
    """创建问答链"""
    # 初始化LLM
    llm = ChatOpenAI(
        model_name="gpt-3.5-turbo",
        temperature=0
    )
    
    # 创建检索器
    retriever = vector_store.as_retriever(
        search_kwargs={"k": 2}  # 返回最相关的2个文档
    )
    
    # 构建RAG链
    qa_chain = RetrievalQA.from_chain_type(
        llm=llm,
        chain_type="stuff",
        retriever=retriever,
        return_source_documents=True  # 返回参考文档
    )
    
    return qa_chain

# 4. 主程序
def main():
    print("🚀 正在初始化RAG系统...")
    
    # 加载文档
    chunks = load_documents()
    print(f"✅ 文档已分割为 {len(chunks)} 个片段")
    
    # 创建向量数据库
    vector_store = create_vector_store(chunks)
    print("✅ 向量数据库创建完成")
    
    # 创建问答链
    qa_chain = create_qa_chain(vector_store)
    print("✅ RAG问答链已就绪\n")
    
    # 交互式问答
    while True:
        question = input("\n📝 请输入问题 (输入'quit'退出): ")
        
        if question.lower() == 'quit':
            break
        
        # 执行问答
        result = qa_chain.invoke({"query": question})
        
        print("\n" + "="*50)
        print(f"🤖 回答:\n{result['result']}")
        print("="*50)
        
        # 显示参考来源
        if result.get('source_documents'):
            print("\n📚 参考来源:")
            for i, doc in enumerate(result['source_documents'], 1):
                print(f"  [{i}] {doc.page_content[:100]}...")

if __name__ == "__main__":
    main()
```

### 运行测试

```bash
python rag_demo.py
```

示例交互：

```
🚀 正在初始化RAG系统...
✅ 文档已分割为 3 个片段
✅ 向量数据库创建完成
✅ RAG问答链已就绪

📝 请输入问题 (输入'quit'退出): 退货期限是多久？

==================================================
🤖 回答:
根据公司退货政策，退货期限分为两种情况：
1. 无理由退货：自购买之日起7天内可申请
2. 质量问题退货：可在30天内申请退货
==================================================

📚 参考来源:
  [1] 公司退货政策（2024年更新）：1. 无理由退货：自购买之日起7天内可申请无理由退货...
  [2] 2. 质量问题退货：发现质量问题可在30天内申请退货...
```

## 优化技巧

### 1. 文本分块策略

```python
from langchain.text_splitter import RecursiveCharacterTextSplitter

# 递归分块，更智能
splitter = RecursiveCharacterTextSplitter(
    chunk_size=500,
    chunk_overlap=100,
    separators=["\n\n", "\n", "。", " ", ""]
)
```

### 2. 混合检索

结合关键词检索和向量检索：

```python
from langchain.retrievers import BM25Retriever, EnsembleRetriever

# BM25关键词检索
bm25_retriever = BM25Retriever.from_documents(documents)
bm25_retriever.k = 2

# 向量检索
vector_retriever = vector_store.as_retriever(search_kwargs={"k": 2})

# 混合检索（权重可调整）
ensemble_retriever = EnsembleRetriever(
    retrievers=[bm25_retriever, vector_retriever],
    weights=[0.5, 0.5]
)
```

### 3. 重排序优化

```python
from langchain.retrievers import ContextualCompressionRetriever
from langchain.retrievers.document_compressors import LLMChainExtractor

# 使用LLM对检索结果重新排序
compressor = LLMChainExtractor.from_llm(llm)
compression_retriever = ContextualCompressionRetriever(
    base_compressor=compressor,
    base_retriever=retriever
)
```

### 4. 提示词工程

```python
from langchain.prompts import PromptTemplate

custom_prompt = """基于以下上下文回答问题。如果上下文中没有相关信息，请明确说明。

上下文：
{context}

问题：{question}

请提供详细且准确的回答："""

PROMPT = PromptTemplate(
    template=custom_prompt,
    input_variables=["context", "question"]
)

qa_chain = RetrievalQA.from_chain_type(
    llm=llm,
    chain_type="stuff",
    retriever=retriever,
    chain_type_kwargs={"prompt": PROMPT}
)
```

## 总结

本篇文章详细介绍了 **RAG（检索增强生成）** 技术的核心概念、工作原理和实践方法。

### 重点回顾

| 要点 | 说明 |
|------|------|
| **什么是RAG** | 将外部知识检索与LLM生成相结合的技术架构 |
| **为什么需要** | 解决LLM知识截止和幻觉问题 |
| **工作原理** | 文档预处理 → 向量化存储 → 相似度检索 → 上下文生成 |
| **核心组件** | Embedding模型、向量数据库、LLM、检索器 |

### 进阶方向

1. **多模态RAG**：支持图片、PDF、表格等多种文档类型
2. **Agentic RAG**：让RAG具备自主决策能力，可多次检索
3. **Graph RAG**：结合知识图谱进行更复杂的推理
4. **本地部署**：使用Ollama等工具实现完全私有化

---

*本文由AI自动生成，每日更新AI技术教程。如有疑问欢迎留言交流！*